# Core — required
# NOTE: Pygame requires Python 3.9–3.13.
# Python 3.14 is not yet supported by pygame as of 2026-04.
# Use: py -3.12 -m pip install pygame  (if you have 3.12 installed)
pygame>=2.1.0

# Optional — only needed if you use URL asset downloading
# Pillow>=9.0.0
# requests>=2.28.0
